export interface Class {
  id?: string;
  name: string;
  trainer_id?: string;
  capacity: number;
  duration_minutes: number;
  description?: string;
  trainer_name?: string;
  created_at?: string;
}
